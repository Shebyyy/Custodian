name: Deploy Custodian to Server

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          command_timeout: 300s
          script_stop: false
          script: |
            set -e
            echo "=== Deploying Custodian (main) ==="

            # Install build tools (needed for better-sqlite3 native compile)
            echo "[1/8] Checking build tools..."
            if ! which make || ! which gcc; then
              echo "Installing build-essential, python3..."
              apt-get update -qq
              apt-get install -y -qq build-essential python3
              echo "OK: build tools installed"
            else
              echo "OK: build tools already available"
            fi

            # Install bun if missing
            echo "[2/8] Checking bun..."
            if ! which bun; then
              echo "Installing bun..."
              curl -fsSL https://bun.sh/install | bash
            fi
            export PATH="$HOME/.bun/bin:$PATH"
            echo "OK: bun available ($(bun --version))"

            # Clone or pull
            echo "[3/8] Fetching code..."
            if [ -d /root/Custodian/.git ]; then
              cd /root/Custodian
              git fetch origin main
              git clean -fd
              git reset --hard origin/main
            else
              rm -rf /root/Custodian
              git clone -b main https://github.com/${{ github.repository }}.git /root/Custodian
            fi
            echo "OK: code fetched"

            # Install deps (clean node_modules to force native recompile)
            echo "[4/8] Installing dependencies..."
            cd /root/Custodian
            rm -rf node_modules
            bun install
            echo "OK: deps installed"

            # Write .env from secrets
            echo "[5/8] Writing .env..."
            printf 'DISCORD_BOT_TOKEN=%s\n' \
              '${{ secrets.DISCORD_BOT_TOKEN }}' \
              > /root/Custodian/.env
            chmod 600 /root/Custodian/.env
            echo "OK: .env written"

            # Setup database
            echo "[6/8] Setting up database..."
            cd /root/Custodian
            bun run db:setup
            echo "OK: database ready"

            # Kill old process
            echo "[7/8] Restarting bot..."
            if [ -f /root/Custodian/data/bot.pid ]; then
              kill $(cat /root/Custodian/data/bot.pid) 2>/dev/null || true
              rm -f /root/Custodian/data/bot.pid
            fi
            sleep 2

            # Start
            mkdir -p /root/Custodian/data
            setsid bun run /root/Custodian/src/index.ts > /root/Custodian/data/bot.log 2>&1 &
            echo $! > /root/Custodian/data/bot.pid
            sleep 5

            # Health check
            echo "[8/8] Health check..."
            if kill -0 $(cat /root/Custodian/data/bot.pid) 2>/dev/null; then
              echo "✅ Custodian is running (PID: $(cat /root/Custodian/data/bot.pid))"
            else
              echo "❌ Custodian failed to start!"
              cat /root/Custodian/data/bot.log
              exit 1
            fi
